import React from 'react';
import { FormGroup, Label, Control, Form } from 'react-bootstrap';

const FormGroupItem = ({ handleChange, label, inputType, inputValue }) => {
  return <FormGroup></FormGroup>;
};

export default FormGroupItem;
